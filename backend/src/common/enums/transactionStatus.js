// ─── Transaction Status Enum ───
export const TransactionStatus = Object.freeze({
  INITIATED: 'INITIATED',
  PENDING: 'PENDING',
  SUCCESS: 'SUCCESS',
  FAILED: 'FAILED',
  BLOCKED: 'BLOCKED',
});
